<h1>InstaRails v2 (now with Geocode!)</h1>

<h2>Your Profile</h2>

<h5 class="mt-3">Name:</h5>
<%= @current_user.profile.full_name %>

<h5 class="mt-3">Email Address:</h5>
<%= @current_user.email %>

<h5 class="mt-3">Address:</h5>
<%= @current_user.profile.full_address %>

<p>
    <%= google_map_image @current_user.profile %>
</p>

<p>
    <%= link_to 'Edit Profile', profile_edit_path, :class => 'btn btn-primary' %>
</p>
